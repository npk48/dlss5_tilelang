# Separate from the unchanged minimal-NR distribution requirements.
# Install matching CUDA builds of this pair if not already present;
# current verified environment is torch2.5.1+cu124/torchvision0.20.1+cu124.
# Do NOT install the upstream VDA requirements wholesale (old Torch/xformers).
numpy
torch==2.5.1
torchvision==0.20.1
Pillow
einops==0.8.0
Flask>=3.0
# Video additionally needs installed FFmpeg + ffprobe (not bundled).
