# Qualification records

`external-assets.json` is the external-runtime/npm WebUI qualification. It includes real-browser GPU processing, token auth, downloads, comparison/zoom/mobile layout and exact HTTP PNG identity.

Other records describe the initial cc83efd SDK migration. Its historical single-EXE embedding evidence is not the current deployment layout. Native numerical component evidence remains applicable where those components have not changed.
