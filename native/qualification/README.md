# Qualification records

- `nr-runtime-spatial.json`: generic NR code assets, five-grid bitwise parity, no new compiles/module loads, separate Engines/streams, paired public DLL performance, and real mixed-size full-pipeline WebUI.

- `dynamic-vda-browser.json`: real WebUI full-pipeline mixed-size matrix using one dynamic VDA pair; output PNG sizes, init/step and geometry-history resets checked.
- `dynamic-vda-min-patch.json`: extreme-aspect minimum-patch path compared with the original model on the same network geometry.
- `model/native_guides/dynamic_vda_comparison.json` and `dynamic_vda_graph_evidence.json`: original FP32 reference comparison and symbolic-spatial graph audit.
- `external-assets.json`: external-runtime/npm WebUI, token auth, downloads, comparison/zoom/mobile and HTTP PNG identity.

Other records describe the initial cc83efd migration. Historical fixed-grid/embedded-EXE evidence is not the current deployment contract. Unchanged numerical components retain their prior validation evidence.
